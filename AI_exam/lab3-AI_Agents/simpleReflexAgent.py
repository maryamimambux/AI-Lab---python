# simple reflex agent
# Acts only on current situation. No memory.

"""

class SimpleReflexAgent:
    def __init__(self):
        # ** No memory needed - simple agent! **
        pass

    def act(self, percept):
        # Decision based ONLY on current input
        if percept == 'Hot':
            return 'Pull hand away'  # ** Rule 1: if hot, pull **
        else:
            return 'Keep hand'       # ** Rule 2: if not hot, keep **

"""

